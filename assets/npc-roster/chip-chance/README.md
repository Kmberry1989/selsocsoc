# Chip Chance

Role: minigame host.

Replace the placeholder GLBs while retaining their filenames, or update `../manifest.json`. Face art belongs in this folder as a PNG or WebP and is applied to `FaceSlot_CC`. Models use meters, +Y up, and face +Z.
